# Screenshots

Drop the README/doc screenshots here, using these exact filenames so they render in the docs.
PNG preferred; landscape; crop to the relevant panel; a dark theme matches the GUI.

| Filename | What to capture |
| --- | --- |
| `hero.png` | The headline shot — the GUI **Live Data (Grouped)** view, or a nice Home Assistant dashboard built from rPDU2MQTT entities. |
| `home-assistant-outlet.png` | A single **outlet device** in Home Assistant — its switch, reboot button, on/off/reboot delays, power-on action, and power/energy sensors. |
| `home-assistant-group.png` | A **OneView group device** in Home Assistant — the Sum/Avg/Min/Max rollup sensors, the mirrored member switches, and the All On / All Off / Reboot All buttons. |
| `gui-config.png` | The GUI **configuration form** (e.g. the PDU section) showing the structured form + left nav. |
| `gui-control.png` | The GUI **Control** tab — per-outlet On/Off/Reboot/Reset + editable label, and the Groups section. |
| `gui-live-data.png` | The GUI **Live Data** tab in **Grouped** view (pivoted, with outlet state). |
| `gui-paths.png` | The GUI **Paths** tab — generated MQTT topic / Prometheus metric / EmonCMS key per measurement. |
| `grafana.png` | A **Grafana** dashboard (or Prometheus graph) built from the `rpdu2mqtt_*` metrics. |
| `emoncms.png` | **EmonCMS** inputs/feeds populated by rPDU2MQTT. |

Optional extras (not yet referenced — add a slot in the README if you include them): the GUI **Diagnostics** tab, the **Overrides** editor with the path preview, the **Export** view, an OIDC login screen.
