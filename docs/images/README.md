# Screenshots

Doc/README screenshots. PNG (a WebP pass is a nice-to-have later); landscape; crop to the relevant panel; the dark theme matches the GUI.

Currently used in the README:

| Filename | What it shows |
| --- | --- |
| `gui-live-data.png` | GUI **Live Data** (Grouped) — the headline shot. |
| `gui-config.png` | GUI **configuration** form (structured form + nav). |
| `gui-control.png` | GUI **Control** tab — per-outlet actions + group actions. |
| `gui-paths.png` | GUI **Paths** tab — generated MQTT/Prometheus/EmonCMS paths (cropped from a tall capture). |
| `home-assistant-outlet.png` | An outlet device in Home Assistant — switch, sensors, delays, power-on. |
| `home-assistant-group.png` | A OneView group device — rollup sensors, member switches, All On/Off/Reboot. |
| `home-assistant-history.png` | A Home Assistant sensor history graph (power over time). |

Not yet captured (add the file here and a slot in the README if you want them): **`grafana.png`** (a Grafana/Prometheus dashboard) and **`emoncms.png`** (EmonCMS inputs/feeds).
