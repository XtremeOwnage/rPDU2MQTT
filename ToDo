Your todo list....

The intervalSeconds should be configurable By Type.

Also, the processing logic, should be confiugrable by type too. 

I applied and enabled autoconfigure, but, appears I have to restart services to get it to take effect. I expect more live reloads.

In addition, creating the feed, is NOT good enough... you have to add the processes.

Such as logging to feed.

![alt text](image.png)

The Energy type, should also have optional setting to enable a Kwh/d, in which case, you would have multiple steps in the process list.

You would first log the energy, and then, convert to Kwh/d, and log again.

![alt text](image-1.png)

The naming standards, should be better adjsutable too. 

The Value selection is currently plain-text. This should be a dropdown...

![alt text](image-2.png)

And, again, instead of just a value- there needs to be some level of allowed customization on a PER METRIC basis. 

ANd- Kwh/d, should be configured with DAILY, and not say, every 10 seconds.

I WOULD like a optional, but defaulted checkbox, so that all feeds are named as an idempotently as possible.... and won't change when the display name changes.

BUT- also, the ability to create virtual feeds, with friendly names, which uses the idempotent feed names as the source.

![alt text](image-3.png)